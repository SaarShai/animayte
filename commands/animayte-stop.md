---
description: Dismiss the animayte pet (closes the floating window and the event daemon)
allowed-tools: Bash(bash:*)
---

Dismiss the animayte pet. Run this and confirm to the user that the pet has been put away:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/animayte" stop
```
